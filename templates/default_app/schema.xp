datasource db {
  provider = "sqlite"
}

// Define your models here, then run: xp migrate
//
// Example:
//
// model User {
//   id        Int      @id @default(autoincrement())
//   username  String   @unique
//   createdAt DateTime @default(now())
// }
